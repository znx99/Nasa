<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title>Objeto de Aprendizagem - Editor HTML</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 20px;
      background-color: #f4f4f4;
    }

    h1 {
      text-align: center;
      color: #333;
    }

    p {
      text-align: center;
      max-width: 600px;
      margin: 0 auto 20px auto;
    }

    #container {
      display: flex;
      justify-content: space-between;
      gap: 10px;
      flex-wrap: wrap;
    }

    textarea {
      width: 48%;
      height: 300px;
      font-family: monospace;
      font-size: 14px;
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 8px;
      background-color: #fff;
      resize: vertical;
    }

    iframe {
      width: 48%;
      height: 300px;
      border: 1px solid #ccc;
      border-radius: 8px;
      background-color: #fff;
    }

    @media (max-width: 800px) {
      textarea, iframe {
        width: 100%;
      }
    }
  </style>
</head>
<body>

  <h1>✏️ Objeto de Aprendizagem: HTML na Prática</h1>
  <p>Digite o código HTML no editor à esquerda e veja o resultado aparecer ao vivo à direita.</p>

  <div id="container">
    <textarea id="editor">
<h1>Olá, mundo!</h1>
<p>Este é meu primeiro site 😄</p>
    </textarea>
    <iframe id="preview"></iframe>
  </div>

  <script>
    const editor = document.getElementById('editor');
    const preview = document.getElementById('preview');

    function atualizarPreview() {
      preview.srcdoc = editor.value;
    }

    editor.addEventListener('input', atualizarPreview);
    atualizarPreview();
  </script>

</body>
</html>
